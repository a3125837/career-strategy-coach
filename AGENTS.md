# 项目持久约束

## 存储位置

- 本项目的唯一根目录是 `F:\AIPro\Career Strategy Coach  职业战略教练`。
- 项目文件、Skill 项目内容、职业档案、日常缓存、日志、测试产物、构建产物、临时文件和生成结果默认不得放在 C 盘；这些内容必须保存在 F 盘。
- C 盘允许读写系统级关键内容，例如操作系统或既有工具所需的认证凭据、CLI 登录信息、系统配置和必要的工具配置；不得借此把项目内容或日常工作文件迁入 C 盘。
- 默认的个人 Codex Skill 目录如果位于 C 盘，不得向其中安装本项目 Skill；本项目 Skill 继续使用用户批准的 F 盘安装方案，除非用户另行明确授权。

## 个人职业信息

- `career-strategy-coach` Skill 本身不得保存用户个人信息、职业档案内容或职业档案路径。
- 职业档案只能保存到用户明确选择的独立工作区。
- 任何职业档案写入前，必须展示解析后的绝对路径和拟创建或更新的文件，并取得用户确认。

## 职业战略教练调用

- `/ZhiYeJiaoLian` 是本项目的项目级文本别名，不是 Codex 原生自定义裸斜杠命令。看到该文本时，必须读取并遵循 `.agents/skills/career-strategy-coach/SKILL.md`。
- `/ZhiYeJiaoLian` 后有文本时，将后续文本作为本次职业问题；只有别名本身时，只询问用户本次想解决的职业问题，不预先启动完整访谈。
- 用户说“帮我分析一下职业路径”，或提出分析职业路径、职业发展、职业规划、跳槽、转岗、转型、路线比较、职业复盘等请求时，也必须调用该 Skill。
- 默认个人职业档案位置为 `F:\AIPro\Career Strategy Coach  职业战略教练\career-profiles\<用户起的名字>`，仓库内对应的 `career-profiles/` 目录不得纳入 Git。创建前先请用户提供一个单独的文件夹名称，再展示解析后的绝对路径及恰好五项标准工作区内容，并等待用户明确确认后才可执行 `init`。
- 用户明确提供其他绝对路径时，尊重该路径；仍须展示解析后的绝对路径、恰好五项标准内容并等待明确确认。
